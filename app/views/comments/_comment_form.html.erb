
<%= form_for ([@recipe, @comment]), :html => {:id => "new-comment"} do |f| %>
<%= f.hidden_field :recipe_id, value: @recipe.id %>
<%= f.text_area :content, class: 'text', placeholder: 'Add a new comment!' %>
<%= f.submit "Add!", class: "btn", :id => "comment-submit" %>

<% end %>
